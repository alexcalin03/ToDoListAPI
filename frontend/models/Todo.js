export default class Todo{
    constructor(id, title, description, dueDate, priority, project_id)
    {
        this.id=id;
        this.title=title;
        this.description=description;
        this.dueDate=dueDate;
        this.priority=priority;
        this.project_id=project_id;
    }
}